package com.ssp.ding;

import org.junit.Before;

/**
 * @author: sunshaoping
 * @date: Create by in 3:50 下午 2020/6/8
 */
public class BaseTest {

    @Before
    public void init() {

/*
        ConverterConfiguration converterConfiguration = new ConverterConfiguration();
        ObjectMapper objectMapper = converterConfiguration.objectMapper();
        ConversionService conversionService = converterConfiguration.conversionService();

        DingConfig dingConfig = new DingConfig();
        dingConfig.setAppKey("dingvjui6xgunxmli4c7");
        dingConfig.setAppSecret("YpyyMICWkbwS4xpxna1D1aRHri1LUvIOanqF7l9BrL6a_AABC0s-kNTs6GTWRk7m");


        DingConfiguration configuration = new DingConfiguration(dingConfig);

        DingConfigStorage dingConfigStorage = configuration.dingConfigStorage();
        DingTalkClientFactory dingTalkClientFactory = configuration.dingTalkClientFactory();

        DingService dingService = configuration.dingService(dingConfigStorage, dingTalkClientFactory);
        Singleton.put(DingService.class.getName(), dingService);

        ContactsConfiguration contactsConfiguration = new ContactsConfiguration(objectMapper, (DingClient) dingService, conversionService);
        converterConfiguration.addConverterConfigurer(contactsConfiguration);

        DingDepartmentService dingDepartmentService = contactsConfiguration.dingDepartmentService();
        DefaultDingRoleService defaultDingRoleService = contactsConfiguration.dingRoleService();
        DingUserService dingUserService = contactsConfiguration.dingUserService();

        Singleton.put(DingDepartmentService.class.getName(), dingDepartmentService);
        Singleton.put(DefaultDingRoleService.class.getName(), defaultDingRoleService);
        Singleton.put(DingUserService.class.getName(), dingUserService);
*/


    }
}
